# MMD 真人式交互测试说明

## 已实现

- 握手：手掌靠近模型左手或右手；控制器模式下需要按住 Grip 或 Trigger。
- 摸头：张开手掌靠近模型头顶，模型会歪头并尝试调用微笑/脸红表情。
- 捏脸：拇指和食指捏合后靠近脸部；控制器模式下可在脸旁按住 Trigger。
- 防误操作：上述交互进行时，整个人物的拖动会暂时锁定，手离开后恢复。
- 自动适配：按 PMX 原始骨骼名寻找头、上臂、肘和手腕，按 Morph 名寻找微笑、脸红和惊讶。
- 降级方式：模型缺少标准骨骼或对应 Morph 时不会闪退，只会减少骨骼动作或表情。

## 不戴头显的测试

1. 用 Unity Hub 打开 `banxia`。
2. 打开 `Assets/Scenes/Prototype.unity`，点击 Play。
3. 等待模型出现。
4. 在左上角依次点 `Handshake`、`Head pat`、`Cheek pinch`。
5. 观察 HUD 的 `Human` 状态、头部动作、手臂动作和表情变化。

自动检查已经覆盖：Unity 编译、XR Hands 依赖、Android OpenXR 手部追踪开关、日文 MMD 骨骼匹配和三种模拟状态。

## Quest 3 真机测试

这是唯一必须由设备完成的部分：

1. 在 Quest 设置中打开手部追踪。
2. 安装并启动 `Builds/Banxia.apk`。
3. 放下控制器，等待系统显示双手。
4. 手掌靠近模型手部测试握手。
5. 张开手掌靠近头顶测试摸头。
6. 在脸旁用拇指和食指做捏合动作测试捏脸。
7. HUD 的 `Human` 应从 `Hands ready` 变成对应的 `active` 状态。
8. 用手掌缓慢拨动带 PMX 动态刚体的发束或裙摆，设备日志应显示 `MmdPhysicsAdapter` 已接入，动态部件应在离手后由原模型弹簧恢复。

如果编辑器按钮有效但真手无反应，只需要反馈 HUD 的 `Human` 文本和 Quest 系统是否显示双手；其余排查由项目日志完成。

## 模型兼容要求

- PMX 最好使用标准 MMD 骨骼名，例如：頭、左腕、左肘、左手首、右腕、右肘、右手首。
- 表情是可选的。常见的笑、にこ、smile、照れ、赤面、blush、驚、surprise 会自动匹配。
- 非标准命名模型仍可显示，但需要后续增加一份骨骼/表情别名配置。

角色侧现在有真实的 PMX 动态刚体碰撞，但用户手上仍没有真实触觉阻力；要做“握住以后有力反馈”，需要额外硬件，Quest 3 手部追踪本身无法提供。

当前动作仍是原型级骨骼反馈，不代表最终的连续身体交互。手臂 IK、接触生命周期、注视、语音和 AstrBot 的后续顺序见 [DEVELOPMENT_ROADMAP_CN.md](DEVELOPMENT_ROADMAP_CN.md)。
