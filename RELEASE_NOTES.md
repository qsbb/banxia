# v0.2.15

- 修复 action-only 对话在已收到 `avatar.intent` 后缺少终帧时长期等待的问题；客户端现在区分首事件、回复停滞和终帧缺失并准确回到 Idle 或报告错误。
- 合法空 `reply.end` 保持成功收口，不重复执行动作；旧轮和 stale `AvatarIntent` 不会刷新当前轮计时。

# v0.2.14

- 物理稳定策略改为按 PMX 关节拓扑选择：具有长串完全锁定平移关节的头发/裙摆链自动保留完整强化，其他重模型继续使用降载策略，不依赖模型名称或文件哈希。
- 动作准备期间恢复实时物理时，从当前骨架重新播种动态刚体并清除旧速度，避免头发和衣物在动作加载完成后被旧姿势瞬间拉扯。
- 模型加载日志记录关节/刚体数量、锁定链稳定保护和最终强化档位，便于定位新模型的物理抖动。
- 增加真实森林莓果 PMX 的拓扑稳定性回归测试。

# v0.2.13

- 补充设备日志自动滚动的持久化与手动翻页回归测试，确保关闭自动滚动后新日志不会抢走当前阅读位置。
- 将语音分块 HTTP 诊断压缩规则提为可测试策略：仅保留首块、失败、慢请求和结束汇总，避免正常 202 分块再次刷屏。
- 增加可选的真实森林莓果 PMX 五元音筛选测试，确认只绑定一组 A/I/U/E/O 标准 Morph，不把口角、笑容或重复元音叠入口型。

# v0.2.12

- 动作目录指纹与 VMD 安全检查移到后台线程，刷新阶段保存已验证元数据；播放时文件未变化便不再在 Unity 主线程重复遍历关键帧。
- 修正 v0.2.10 引入的动作冷加载时序回归：文件确实变化而必须复检时，先暂停森林莓果实时 Bullet，再进行同步检查和转换；动作 QA 现在单独记录目录扫描耗时与物理丢弃。
- 模型切换期间暂停旧模型物理，新 PMX 层级在完整构建前保持 inactive，避免旧、新两套 MMD 运行负载与导入切片叠加。
- 语音元音改为连续交叉淡化，语音结束后沿最后音素平滑闭嘴；触碰表情改为可逆增量层，不再覆盖 VMD、情绪和口型 Morph。

# v0.2.11

- 新增仅供 ADB 调试使用的有界性能 QA：可指定模型、预热和采样时长，并临时组合物理档、手部接触和描边，结束后恢复原设置与模型选择并自动退出。
- 性能 QA 输出 5/30 秒 FPS、帧时 P50/P95/max、OpenXR CPU/GPU、合成器丢帧、物理丢弃，并为 MMD 采样、骨骼/IK、物理管线、回写、SDEF、手部接触和描边提交输出采样窗口 P50/P95。
- `hand_contact=off` 现在是完整 A/B：保留视觉手模型，同时停用精确接触、`Physics.SyncTransforms()` 和外部 Bullet 手部探针；结束后恢复用户原设置。
- 未佩戴或失焦时不再把 0 个有效样本报告成成功结果；模型加载和预热阶段不进入正式统计。
- 关节重模型的性能物理档移除额外锁定平移约束副本，平衡档仍保留一份强化；轻模型和默认平衡体验不变。

# v0.2.10

- 自定义 VMD 动作增加跨 App 重启的内容寻址磁盘缓存；首次转换后后台保存无场景引用的骨骼与 Morph 曲线，后续启动只重建当前模型绑定，不再重复 Bullet/IK 烘焙。
- 缓存键覆盖 PMX/VMD 内容、实际绑定拓扑、Unity/应用/转换器版本和物理烘焙参数；模型、动作或转换语义变化都会自动失效。
- 缓存文件使用版本化二进制协议、payload SHA-256、严格长度/计数校验和原子写入；损坏或不兼容时删除单项并回退正常转换。
- Quest 缓存上限为 256 MiB / 8 项 / 单项 96 MiB；设备剩余空间低于 1 GiB 时停止写入，低空间不会影响动作播放。
- PMX 内容指纹在模型首次读取时按帧预算计算并随解析缓存复用；切换或卸载模型时同步更新，避免跨模型复用错误曲线。

# v0.2.9

- 森林莓果等重物理模型在冷准备动作时使用独立的 0.8ms 主线程转换切片，并记录实际切片预算；实时 Bullet 继续在准备期间暂停并清空累计时间，降低动作等待时的长帧和物理丢弃。
- OpenXR 72Hz 请求在首次窗口失败后每 30 秒低频重试，不再因 XR 显示器启动较晚而长期停在 60Hz。
- 语音口型和表情改为叠加在当前 VMD Morph 上的可逆增量层，相邻音素使用有界交叉淡化，停止说话后准确恢复上游脸部动画。
- 思考、回复和动作期间的注视作为头部编舞后的受限增量持续生效；舞蹈仍保留动作自身头部姿态，触碰保持最高优先级。
- 设备诊断增加口型实测值、动作准备切片和可暂停的日志翻页；可选择自动滚动，关闭后新日志不会抢走当前阅读位置。
- 修正舞蹈意图早于模型绑定时被拒绝的问题，改为等待角色绑定后继续同一动作回执链。

# v0.2.8

- Android PSS 与温度查询改为附着 JNI 的后台任务；主线程只发布已经完成的结果，性能页不再每 30 秒制造一次系统查询长帧。
- OpenXR 72Hz 请求窗口延长为 10 秒低频重试；性能监控缓存当前 MMD/手追组件，不再每帧重复查询组件。
- 对话注视改为在 MMD/动作头姿之后叠加受限偏移，物理触碰仍拥有最终控制权；聆听、思考和语音回复期间持续看向用户，全身动作继续保留自身头部编舞。
- 增加有界的设备端 VMD QA 场景：按模型/动作索引执行冷缓存与热缓存准备，记录各阶段耗时、yield、物理暂停和丢弃增量，随后恢复原模型并退出。
- 修正 QA/应用退出时模型 Renderer 与 Avatar 先销毁导致的 Morph/idle 空引用；VMD 收口现在可重复执行且退出阶段不再跨组件派发动作。

# v0.2.7

- 修正 OpenXR 刷新率切换竞态：请求 72Hz 后不再把运行时短暂上报的旧 60Hz 回写为应用帧率，并等待刷新率真正生效。
- 性能页保留每秒 OpenXR 与帧时采样，但把 Android PSS/温度等系统调用降为每 30 秒一次，避免诊断面板本身干扰森林莓果等重物理模型。
- 首次准备 VMD 时使用 2ms 主线程切片并临时租用实时物理暂停；转换完成后恢复并清空累计时间，脸部轨道不再重复烘焙身体物理，后续动作复用当前模型的骨骼与 Morph 路径缓存。
- 修正森林莓果嘴型：仅绑定标准元音 Morph，语音口型在 VMD 之后写入且不再叠加下颌骨双重开口；无音素时间线时使用稳定元音并保留平滑幅度过渡。

# v0.2.6

- 修正 Quest 帧节奏：显式跟随 XR 实际刷新率并在恢复焦点后重施目标帧率，避免质量档 VSync 导致半速提交。
- 修正物理丢弃诊断重复累计；暂停、离头和零时间同步不再重复计入旧丢弃值。
- 对话处于聆听、思考或语音回复时持续平滑注视用户；动作执行期间保留动作自身的头部姿态，避免多个组件互相覆盖。
- 放宽自主动作选择到有明确情绪或互动语境的轻微动作，同时保留每轮一个动作、客户端能力白名单和真实回执门禁。

# v0.2.5

- 森林莓果等重物理模型默认使用独立的平衡物理档：60Hz/2 子步、单份锁定平移强化，并保留逐帧手部接触；画质和 MMD 物理可分别手动选择。
- 未佩戴或失焦时暂停 Bullet 并排除失真样本；性能页增加 5/30 秒窗口、OpenXR 指标及骨骼/IK、Bullet、回写、SDEF、手部接触和描边提交耗时。
- PMX 导入在 Unity 2022 上改用真实下一帧调度，并在冷启动恢复模型前先提交首帧，避免 `Task.Yield` 同帧续跑及 OpenXR 启动时间被计入模型长帧。
- 描边改为复用原 SkinnedMeshRenderer 的 URP 额外 Pass，不再复制渲染器或逐帧同步 Morph。
- 动作协议支持受约束的 `method / parameters / transition / source`、客户端能力协商、自然下蹲和完整执行回执。

# v0.2.4

- 增加结构化 AI 动作、下蹲、性能诊断、重模型物理降载、手部碰撞粗筛与离屏物理暂停。
- 目标刷新率固定请求 72Hz，平衡画质使用 2x MSAA 与较短阴影距离。

# v0.2.3

- 支持后端异步快速动作与有序执行回执，内置动作、导入 VMD、拒绝和打断均按真实生命周期上报。
- 口型按实际可听音频的 DSP 时间推进；可消费音素时间线，缺失时平滑回退到 RMS 驱动。
- 手部可视模型始终跟随现实追踪，独立的扫掠、穿透与 UMT 外部球体负责影响头发、脸部和肢体。
- PMX 读取、网格、材质、纹理和 Morph 构建按帧预算执行；快速换模会取消旧读取/构建并清理部分资源，同时保留短时解析缓存与低内存释放。
- 修正透明眼部材质的 URP 颜色和深度写入兼容，不修改用户导入的原始模型。
- 上次模型改用相对 `MmdModels` 的稳定标识，兼容 Android 外部存储路径别名，并在存储暂时不可用时保留选择。
- 多 PMX 包按各自目录解析贴图；同几何的不同贴图变体保持独立可选，歧义贴图不再跨目录猜测。
- 思考期间持续看向用户，移除菜单内置动作按钮，动作选择统一交给对话或自定义动作列表。

# v0.2.2

- 新增统一动作仲裁，避免待机、触碰、后端动作和导入 VMD 相互抢占。
- 改进手追物理：多探针接触、Poke 生命周期、UMT Bullet 代理与手柄/手追切换恢复。
- 自定义舞蹈支持推荐与下一动作选择、缓存诊断、结束姿势保持和自然回待机。
- 新增可选 MRUK 房间语义投影，并保留 ARPlane 降级路径和稳定放置候选。
- 新增脱敏房间语义上报，只传有界物体计数与能力状态；追踪丢失后停止续租。
- 扩展设备内中文诊断，覆盖动作来源、缓存、手部触碰、空间能力和链路阶段。
