# Runtime PMX sample

This folder is a local smoke-test sample. ForestBerry.pmx is accompanied by the texture files it references so the runtime loader can copy the complete set to persistent storage on Android.

The sample is not a redistribution grant. Keep the PMX and textures local unless the original model author permits redistribution.